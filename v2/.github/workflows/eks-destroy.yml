name: Destroy EKS + Cine
run-name: "Destroy EKS + Cine (${{ github.actor }})"

on:
  workflow_dispatch:

env:
  AWS_REGION: ${{ vars.AWS_REGION }}
  TF_CLOUD_ORGANIZATION: ${{ secrets.TFC_ORG }}
  TF_TOKEN_app_terraform_io: ${{ secrets.TFC_TOKEN }}

jobs:
  # ─────────────────────────────────────────────
  # Job 1 – Remove Kubernetes resources
  # ─────────────────────────────────────────────
  destroy-k8s:
    name: Remove Kubernetes resources
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false

      - name: Terraform Init (to read outputs)
        working-directory: nginx-eks/terraform/eks
        run: terraform init

      - name: Get cluster info from Terraform outputs
        id: tf-output
        working-directory: nginx-eks/terraform/eks
        run: |
          echo "cluster_name=$(terraform output -raw cluster_name)"     >> $GITHUB_OUTPUT
          echo "cluster_region=$(terraform output -raw cluster_region)" >> $GITHUB_OUTPUT

      - name: Update kubeconfig
        run: |
          aws eks update-kubeconfig \
            --region ${{ steps.tf-output.outputs.cluster_region }} \
            --name   ${{ steps.tf-output.outputs.cluster_name }}

      - name: Setup Helm
        uses: azure/setup-helm@v4

      - name: Delete Kubernetes manifests
        run: |
          kubectl delete -f nginx-eks/k8s/ingress.yaml   --ignore-not-found
          kubectl delete -f nginx-eks/k8s/app.yaml        --ignore-not-found
          kubectl delete -f nginx-eks/k8s/namespace.yaml  --ignore-not-found

      - name: Uninstall NGINX Ingress Controller
        # FIX-04: release name must match what deploy installed: "nginx-ingress" in namespace "nginx-ingress"
        run: |
          helm uninstall nginx-ingress -n nginx-ingress --ignore-not-found || true
          kubectl delete namespace nginx-ingress --ignore-not-found

      - name: Wait for LoadBalancers to be released
        run: |
          echo "Waiting for AWS to release the LoadBalancers..."
          sleep 30

  # ─────────────────────────────────────────────
  # Job 2 – Destroy infrastructure with Terraform
  # ─────────────────────────────────────────────
  terraform-destroy:
    name: Terraform Destroy
    runs-on: ubuntu-latest
    needs: destroy-k8s

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false

      - name: Terraform Init
        working-directory: nginx-eks/terraform/eks
        run: terraform init

      - name: Force-unlock workspace (if locked from a previous failed run)
        working-directory: nginx-eks/terraform/eks
        run: |
          WORKSPACE_ID=$(curl -sf \
            --header "Authorization: Bearer ${{ secrets.TFC_TOKEN }}" \
            --header "Content-Type: application/vnd.api+json" \
            "https://app.terraform.io/api/v2/organizations/${{ secrets.TFC_ORG }}/workspaces/nginx-eks" \
            | jq -r '.data.id')

          if [[ -z "${WORKSPACE_ID}" || "${WORKSPACE_ID}" == "null" ]]; then
            echo "No se pudo obtener el workspace ID. Continuando sin desbloquear."
            exit 0
          fi

          LOCKED=$(curl -sf \
            --header "Authorization: Bearer ${{ secrets.TFC_TOKEN }}" \
            --header "Content-Type: application/vnd.api+json" \
            "https://app.terraform.io/api/v2/workspaces/${WORKSPACE_ID}" \
            | jq -r '.data.attributes.locked')

          if [[ "${LOCKED}" == "true" ]]; then
            echo "Workspace bloqueado. Ejecutando force-unlock..."
            curl -sf \
              --header "Authorization: Bearer ${{ secrets.TFC_TOKEN }}" \
              --header "Content-Type: application/vnd.api+json" \
              --request POST \
              "https://app.terraform.io/api/v2/workspaces/${WORKSPACE_ID}/actions/force-unlock"
            echo "Workspace desbloqueado."
          else
            echo "Workspace libre. Continuando."
          fi

      - name: Terraform Destroy
        working-directory: nginx-eks/terraform/eks
        run: |
          terraform destroy \
            -var="aws_region=${{ vars.AWS_REGION }}" \
            -auto-approve

      - name: Confirm destruction
        run: |
          echo "──────────────────────────────────────────────"
          echo "✅ Todos los recursos han sido destruidos:"
          echo "   - Ingress / Deployment / Service / Namespace"
          echo "   - NGINX Ingress Controller (nginx-ingress)"
          echo "   - Cluster EKS"
          echo "   - VPC, subnets, NAT Gateway, security groups"
          echo "──────────────────────────────────────────────"
